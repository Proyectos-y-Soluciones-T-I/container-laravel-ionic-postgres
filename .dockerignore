# Never send these to Docker daemon

# Source deps (large, rebuilt in container)
ayudando/server/vendor/
ayudando/server/node_modules/
ayudando/frontend/node_modules/
ayudando/frontend/.angular/
ayudando/**/*.sql
ayudando/**/*.tar

# Git and secrets
.git/
.env

# Internal tooling (never needed inside image)
.claude/
.atl/
.github/

# Docs and CI config
*.md
Makefile
