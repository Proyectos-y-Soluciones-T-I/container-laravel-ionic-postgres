/** @type {import('tailwindcss').Config} */
module.exports = {
  // Scan only the dashboard HTML — keeps the output CSS lean
  content: ['./index.html'],
  theme: {
    extend: {},
  },
  plugins: [],
}
