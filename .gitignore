# --- Fuente de proyectos (repos separados) ------------------------------------
src/
secrets/

# --- Variables de entorno (credenciales) -------------------------------------
.env
!.env.example

# --- Herramientas internas (no deben ir al repo público) ---------------------
.claude/
.atl/

# --- Override local (GPU, port overrides, etc.) — no va al repo ---------------
docker-compose.override.yml

# --- Volúmenes y datos generados por Docker -----------------------------------
docker/data/
*.log

# --- Node (dev tooling — husky/commitlint) ------------------------------------
node_modules/
package-lock.json

# --- OS / Editor -------------------------------------------------------------
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo
*~
